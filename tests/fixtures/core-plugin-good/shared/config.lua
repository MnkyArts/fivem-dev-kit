-- Loaded in both VMs. `Config` is the one intentional global of a core plugin.

Config = {
    Debug = false,
    Shop = { coords = vector3(25.7, -1347.3, 29.49), radius = 2.0 },
    SnackPrice = 5,
    SnackHeal = 20,
}
