---
description: Search or verify FiveM natives and local Cfx.re documentation with fxref.
agent: fivem
---

Load the `fivem-reference` skill using the `skill` tool. Use `fxref` rather than memory to answer:

$ARGUMENTS
