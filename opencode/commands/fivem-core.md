---
description: Work on the core framework or a core plugin using its mandatory rulebook.
agent: fivem-core
---

Load the `fivem-core` skill using the `skill` tool. Read the relevant `resources/core/AGENTS.md` and
`DESIGN.md` sections, then perform this request:

$ARGUMENTS
